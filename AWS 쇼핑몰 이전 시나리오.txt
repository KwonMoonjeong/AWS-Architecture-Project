 A 쇼핑몰 회사의 클라우드 이전 함

  AWS / Azure 클라우드 중에 하나로 이전하기를 원함

  A 쇼핑몰
   Web : 500 대 - C2 / M8 / Windwos OS / IIS  (초당 500 명 / 평균 200 명 / 1TB) AWS / Azure 계산기 수정
   스케일링 과 ALB / NLB 금액 으로 변경
  API 서버 : 300 대 - C8 / M64 / Widows OS / .NET
  Microsoft SQL Server 2017 Enterprise 2 대 / C64 / 256G  2대
                                                     18 대 / C16 / 64G 18 대
  인터넷 -> 웹서버 -> API 서버 -> DB 읽기 전용 요청 -> API 서버 응답 ->  웹서버 -> 인터넷
   주기적으로 메인 DB와 읽기전용 DB와 통신(복제 기능)
                  메인 DB는 클러스터로 구성
               10TB 메인 DB -2대가 똑같이 10 TB 공유
               2TB 읽기 전용 DB - 18대 2TB 씩 사용(1대당 2TB)
Network  CIDR / 계산기 작성(네트워크는 기본값 1G만 책정) : 서울 지역 한정
  임원
  영업
  MD(쇼피몰 PD)
  개발
  DB팀
  엔지니팀
   고개지원팀(소비자)
시스템 접근 권한만 구성할 것
DB 팀 : DB 서버와 DB서버와 관련된 것만 접근함
엔지니어 : 모든 서버에 대한 시스템 접근 권한
개발 팀 : API 서버와 WEB 서버만 접근하고 DB는 접근 못함
 WEB = 80/443  : API = 9000 : MS SQL는 기본 포트
1시 15분까지 점심식사하고 1시 30분에 질문 답변합니다.